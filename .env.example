# Use an AWS profile with read-only permissions for local development.
# Do not place access keys or other secrets in this file.
AWS_PROFILE=sentinel-readonly
AWS_REGION=us-east-1

SENTINEL_LOG_LEVEL=INFO
